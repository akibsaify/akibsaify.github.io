<!-- SEO: title -->
<title>{{ pageTitle if pageTitle else site.name }}</title>

<!-- Core meta -->
<meta name="description" content="{{ pageDescription if pageDescription else site.description }}">
<meta name="robots" content="index, follow">
<link rel="canonical" href="{{ site.url }}{{ page.url }}">

<!-- Open Graph -->
<meta property="og:type" content="{{ ogType if ogType else 'website' }}">
<meta property="og:title" content="{{ pageTitle if pageTitle else site.name }}">
<meta property="og:description" content="{{ pageDescription if pageDescription else site.description }}">
<meta property="og:url" content="{{ site.url }}{{ page.url }}">
<meta property="og:site_name" content="{{ site.name }}">
{% if ogImage %}
<meta property="og:image" content="{{ site.url }}{{ ogImage }}">
<meta property="og:image:width" content="1200">
<meta property="og:image:height" content="630">
{% endif %}
<meta property="og:locale" content="en_IN">

<!-- Twitter Card -->
<meta name="twitter:card" content="summary_large_image">
<meta name="twitter:title" content="{{ pageTitle if pageTitle else site.name }}">
<meta name="twitter:description" content="{{ pageDescription if pageDescription else site.description }}">
{% if ogImage %}<meta name="twitter:image" content="{{ site.url }}{{ ogImage }}">{% endif %}
